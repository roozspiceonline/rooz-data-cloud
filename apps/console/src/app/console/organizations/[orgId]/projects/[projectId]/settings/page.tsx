import { SectionPlaceholder } from "@/components/section-placeholder";

export default function Page() {
  return <SectionPlaceholder title="Project settings" description="Project configuration and role-aware controls will consume authoritative backend permissions." status="foundation" />;
}
